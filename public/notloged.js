// import { response } from "express"

window.addEventListener('load', () => {
    const back = () => {
        apicall('GET', '/notMember', response => {

            console.log(data)
        })
    }
})